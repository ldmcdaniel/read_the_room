'use strict';

describe('myApp.group_view module', function() {

  beforeEach(module('myApp.GroupView'));

  describe('group_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var GroupCtrl = $controller('GroupCtrl');
      expect(GroupCtrl).toBeDefined();
    }));

  });
});