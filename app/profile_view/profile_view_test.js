'use strict';

describe('myApp.profile_view module', function() {

  beforeEach(module('myApp.ProfileView'));

  describe('profile_view controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ProfileCtrl = $controller('ProfileCtrl');
      expect(ProfileCtrl).toBeDefined();
    }));

  });
});